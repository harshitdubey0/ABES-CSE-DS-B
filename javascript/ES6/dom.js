const div = document.createElement("div"); // Create a new div element 
const h1 = document.createElement("h1"); // Create a new h1 element 
 h1.innerText ="Python create AI  " ;// Set the text content of the h1 element 
div.appendChild(h1); // Append the h1 element to the div

const id = document.getElementById("root"); // Get the element with id "container"
id.appendChild(div); // Append the div to the container element